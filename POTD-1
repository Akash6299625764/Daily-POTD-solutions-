class Solution {
public:
    int scoreOfString(string s) {
        int a=0;
        for(int i=0;i<s.length()-1;i++)
        {
            int x=s[i]-s[i+1];
            a=a+abs(x);
        }
        return a;
    }
};
