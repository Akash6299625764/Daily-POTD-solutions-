#include <bits/stdc++.h>
using namespace std;
int main()
{
  string a;
  int n;
  cin >> n;
  cin >> a;
  unordered_set<char> s;
  for (int i = 0; i < a.length(); i++)
  {
    s.insert(tolower(a[i]));
  }
  if (s.size() == 26)
    cout << "YES";
  else
    cout << "NO";
  return 0;
}
